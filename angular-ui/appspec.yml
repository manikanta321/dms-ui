version: 0.2
os: linux
files:
  - source: /
    destination: /home/ubuntu/dist/angular-ui
permissions:
  - object: /home/ubuntu/dist/angular-ui
    pattern: "**"
    owner: root
    group: root

hooks:
  BeforeInstall:
    - location: scripts/dependencies.sh
      timeout: 300
      runas: root
  ApplicationStart:
    - location: scripts/start.sh
      timeout: 300
      runas: root
